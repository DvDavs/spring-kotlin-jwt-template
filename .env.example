# ============================================
# GeoFibra - Environment Variables Template
# ============================================
# Copy this file to .env and update the values
# Complete configuration file for the Spring Boot application

# ============================================
# JWT Configuration (REQUIRED)
# ============================================
# Secret key for JWT token signing (minimum 256 bits recommended)
# IMPORTANT: Change this in production! Use a strong, random secret
JWT_SECRET=your-jwt-secret-here-at-least-256-bits

# ============================================
# Database Configuration (REQUIRED)
# ============================================
# PostgreSQL connection settings
# For Docker: use postgres-db as host
# For local development: use localhost:5501
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres-db:5432/template_db
SPRING_DATASOURCE_USERNAME=evolution
SPRING_DATASOURCE_PASSWORD=evolution123

# ============================================
# Email Configuration (REQUIRED)
# ============================================
# Email settings for password reset and notifications
# For Gmail: Enable 2-Step Verification and generate an App Password
# Visit: https://myaccount.google.com/apppasswords

# Your Gmail address
MAIL_USERNAME=your-email@gmail.com

# Gmail App Password (16 characters, no spaces)
# NEVER use your real Gmail password!
MAIL_PASSWORD=your-app-password-here

# ============================================
# Email Configuration (OPTIONAL)
# ============================================
# SMTP server configuration
# Default values are provided, uncomment to customize

# SMTP host (default: smtp.gmail.com)
MAIL_HOST=smtp.gmail.com

# SMTP port (default: 587 for TLS)
MAIL_PORT=587

# Email provider strategy (default: google)
# Options: google, mailersend, sendgrid (requires implementation)
EMAIL_PROVIDER=google

# ============================================
# Frontend Configuration (OPTIONAL)
# ============================================
# URL of your frontend application
# Used for CORS and email links (password reset, etc.)
FRONTEND_URL=http://localhost:3000

# ============================================
# IMPORTANT NOTES
# ============================================
# 1. NEVER commit .env file to version control with real credentials
# 2. Keep .env in your .gitignore file
# 3. Change all default passwords in production
# 4. Use strong, randomly generated secrets in production
# 5. For Gmail: Always use App Passwords, never your real password
#
# Default Users (Development Only - Change in Production):
# - david@davscode.com / 4dm1n1straT0r123$ (MASTER)
# - denisse@davscode.com / 4dm1n1straT0r123$ (ADMIN)
# - moises@davscode.com / 4dm1n1straT0r123$ (ADMIN)
# - carlos.garcia@davscode.com / 4dm1n1straT0r123$ (USER)
# - ana.martinez@davscode.com / 4dm1n1straT0r123$ (USER)
